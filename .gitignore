node_modules/
dist/
coverage/
*.log
.DS_Store

# Secrets / local env (never commit — contains API keys)
.env
.env.*

# Local editor/IDE
.vscode/
.idea/

# Local scratch notes (never published)
.notes.md

# zipmem local memory (this repo dog-foods zipmem; keep memory local by default)
.zipmem/

# Demo source/working files — only the published zipmem-demo.gif is committed
zipmem-video.mp4
Enhancer-Ultra HD-zipmem-video.mp4
zipmem-demo.mp4
